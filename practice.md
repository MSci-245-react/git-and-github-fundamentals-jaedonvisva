# Weird Edge Case

In Lecture 1, I learned about JavaScript’s eight data types and how `typeof` is used to inspect them. One surprising edge case is that `null` is reported as an object.

- JavaScript has eight data types
- The `typeof` operator helps identify them

```javascript
typeof null
```

```
object
```
